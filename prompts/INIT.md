# Retired representation-test prompt

This filename is retained for existing references only. The previous experimental
workflow permitted axiomatized placeholders and is not appropriate for this project.

Use `prompts/FORMALIZE_PAPER.md` with `prompts/RECURSIVE_FORMALIZATION.md` instead.
Ground definitions in the pinned Mathlib, keep temporary proof debt visible, and
never introduce project axioms. A statement skeleton is an intermediate stage,
not a completed formalization.
